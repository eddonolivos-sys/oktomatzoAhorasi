# Domain for production (Caddy auto-configures HTTPS)
DOMAIN=plataforma.ejemplo.com

# JWT secret (generate with: openssl rand -base64 32)
JWT_SECRET=change-this-to-a-secure-random-string

# Optional: enable debug mode
DEBUG=false
