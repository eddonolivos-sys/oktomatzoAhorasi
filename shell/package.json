{
  "name": "@plataforma/shell",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "lit": "^3.2.0",
    "lit-html": "^3.2.0",
    "three": "0.170.0",
    "@plataforma/shell-protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/three": "0.170.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.0",
    "vitest": "^3.2.0"
  }
}
